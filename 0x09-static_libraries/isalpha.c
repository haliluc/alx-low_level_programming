#include "main.h"

/**
 * _isalpha - prints only alphabets
 * @c: input
 *
 * Return: Always 0 (success)
 */

int _isalpha(int c)
{
	if (c >= 'a', 'A' && c <= 'z', 'Z')
		return (c)

			return (0)
}


